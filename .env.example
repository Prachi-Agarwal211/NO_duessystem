# ENVIRONMENT VARIABLES TEMPLATE
# Copy this file to .env.local and fill in your actual values

# ========================================
# SUPABASE CONFIGURATION (Required)
# ========================================
NEXT_PUBLIC_SUPABASE_URL=https://your-project.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-key-here
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key-here

# ========================================
# JWT SECRET (Required)
# ========================================
# Generate a secure secret: openssl rand -hex 32
JWT_SECRET=your-jwt-secret-here-minimum-32-characters

# ========================================
# EMAIL CONFIGURATION - SMTP (Required)
# ========================================
# For Gmail: Use App Password (not regular password)
# See: https://support.google.com/accounts/answer/185833
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER=your-email@jecrcu.edu.in
SMTP_PASS=your-app-password-here
SMTP_FROM=JECRC No Dues <noreply.nodues@jecrcu.edu.in>

# ========================================
# EMAIL QUEUE CONFIGURATION (Optional)
# ========================================
EMAIL_QUEUE_BATCH_SIZE=50
EMAIL_MAX_RETRIES=3
CRON_SECRET=your-random-secret-for-cron-auth

# ========================================
# APPLICATION URL (Required for Production)
# ========================================
NEXT_PUBLIC_BASE_URL=http://localhost:3000
# Production: https://your-domain.vercel.app

# ========================================
# NOTES:
# ========================================
# 1. Remove any trailing spaces from email addresses
# 2. SMTP_USER and SMTP_PASS are REQUIRED for email functionality
# 3. For Gmail, enable 2FA and create an App Password
# 4. Department emails are managed in the database (profiles table)
# 5. Never commit .env.local to version control